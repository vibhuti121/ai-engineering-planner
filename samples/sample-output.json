{
  "markdown": "# Implementation Plan\n\n_Source: `sample-prd.pdf` · 2 pages · 17 tasks · 14 requirements · generated by `cli`/`claude-sonnet-5` · plan id `44d249ee66ea861a`_\n\n## Summary\n\nSplitTab is a mobile-first web app for small groups to log shared expenses, see always-accurate per-member balances, and get the minimum set of payments that settles the group. The hardest part is the money model: every amount and split must be integer minor units that reconcile exactly to the cent across equal/exact/percentage splits, edits, and a settlement algorithm whose correctness is proven with property-based tests.\n\n## Requirements\n\n| ID | Category | Requirement |\n|---|---|---|\n| FR-1 | auth | A user signs up with email and password, and signs in to receive a session. Passwords are stored hashed. Sessions expire after 30 days. |\n| FR-2 | groups | A signed-in user can create a group with a name and a base currency. The creator becomes the owner and the first member. There is exactly one owner and ownership can be transferred; the owner can rename the group and remove members. |\n| FR-3 | invitations | An owner can invite a member by email. The invitation produces a single-use link valid for 7 days. Accepting it adds the user to the group. |\n| FR-4 | expenses | A member records an expense with description, amount, date, payer, and shared members, split equally, by exact amounts, or by percentage shares. Splits must sum to the total; a mismatch is rejected with the difference. |\n| FR-5 | expenses | Any member can edit or delete an expense. Every change is recorded in an immutable audit trail showing who changed what and when. Balances recompute after any edit. |\n| FR-6 | balances | Each member sees, for every group, what they owe and what they are owed, per counterparty. The sum of all balances in a group is always zero. |\n| FR-7 | settlement | A member can ask for a settlement plan: the minimum number of payments that brings every balance to zero. Recording a settlement payment updates balances the same way an expense does. |\n| FR-8 | notifications | A member receives an email when added to a group, when an expense involving them is recorded, and a weekly digest of open balances. Each is individually switchable. |\n| FR-9 | export | A member can export a group's expense history as CSV, covering an optional date range, with one row per expense and one column per member's share. |\n| FR-10 | activity | A group has a reverse-chronological activity feed of expenses, edits, settlements, and membership changes, paged at 50 entries. |\n| NFR-1 | money | Money is never stored or computed in floating point. All amounts are integer minor units, and every split must reconcile to the cent. |\n| NFR-2 | performance | A group of 20 members with 5,000 expenses loads its balance view in under 500 ms at the 95th percentile. |\n| NFR-3 | security | Every write is authorised against group membership. A user must not be able to read or alter a group they do not belong to, including by guessing an id. |\n| NFR-4 | testing | The settlement algorithm is covered by property-based tests asserting that resulting balances are all zero and no member pays more than they owed. |\n\n## Execution order\n\nOrder is computed from the dependency graph by topological sort — the model proposes dependencies, never an order. Tasks in the same wave have no dependency between them and can be run in parallel.\n\n| # | Wave | Task | Complexity | Depends on |\n|---|---|---|---|---|\n| 1 | 1 | **Add email/password signup endpoint** (`auth-signup-api`) | M | — |\n| 2 | 2 | **Add login endpoint issuing 30-day session** (`auth-login-session`) | M | `auth-signup-api` |\n| 3 | 3 | **Add session-authentication middleware** (`auth-middleware`) | S | `auth-login-session` |\n| 4 | 4 | **Add group creation endpoint and schema** (`groups-create-api`) | M | `auth-middleware` |\n| 5 | 4 | **Add per-notification-type preference settings** (`notifications-preferences-api`) | M | `auth-middleware` |\n| 6 | 5 | **Add group-membership authorization middleware** (`membership-authorization`) | S | `groups-create-api` |\n| 7 | 6 | **Add owner-only rename, remove, transfer endpoints** (`group-owner-management-api`) | M | `membership-authorization` |\n| 8 | 6 | **Add single-use 7-day invite links** (`group-invitations-api`) | M | `membership-authorization` |\n| 9 | 6 | **Add expense creation with three split types** (`expenses-create-api`) | L | `membership-authorization` |\n| 10 | 7 | **Add expense edit/delete with audit trail** (`expenses-edit-delete-api`) | M | `expenses-create-api` |\n| 11 | 7 | **Add per-counterparty group balances endpoint** (`balances-view-api`) | L | `expenses-create-api` |\n| 12 | 7 | **Send group-added and expense-recorded emails** (`notifications-event-emails`) | M | `group-invitations-api`, `expenses-create-api`, `notifications-preferences-api` |\n| 13 | 7 | **Add CSV export of group expense history** (`csv-export-api`) | S | `expenses-create-api` |\n| 14 | 8 | **Implement minimum-payment settlement algorithm** (`settlement-plan-algorithm`) | L | `balances-view-api` |\n| 15 | 8 | **Add endpoint to record a settlement payment** (`settlement-record-payment-api`) | M | `balances-view-api` |\n| 16 | 8 | **Add weekly open-balances digest email job** (`weekly-digest-job`) | M | `balances-view-api`, `notifications-preferences-api` |\n| 17 | 9 | **Add paginated group activity feed** (`activity-feed-api`) | M | `group-owner-management-api`, `group-invitations-api`, `expenses-edit-delete-api`, `settlement-record-payment-api` |\n\n## Tasks\n\n### 1. Add email/password signup endpoint\n\n- **id:** `auth-signup-api`\n- **complexity:** M (~half a day) — Introduces the new users table plus the code that reads and writes it, matching the M clause.\n- **dependencies:** none\n- **requirements:** FR-1\n- **wave:** 1\n\nCreate POST /api/auth/signup that creates a user with a bcrypt-hashed password. Adds the users table. Acceptance: valid signup returns 201 and a users row whose password_hash is not the plaintext; duplicate email returns 409; password under 8 chars returns 400.\n\n<details><summary>Agent prompt</summary>\n\n```text\nImplement user signup. Create a `users` table (id UUID PK, email TEXT UNIQUE NOT NULL, password_hash TEXT NOT NULL, created_at TIMESTAMPTZ) and a POST /api/auth/signup endpoint accepting {email, password}. Hash the password with bcrypt (cost >=10); never store or log plaintext. Validate email format and password length >=8, returning 400 with a message on failure. Return 409 if the email already exists. On success return 201 with the created user's id and email (never the hash). Done when: a valid signup returns 201 and creates a users row with a bcrypt hash; a duplicate email returns 409; a short password returns 400.\n\nAlready done (do not rebuild): nothing — this is a starting task.\nNot your scope (handled separately): Add login endpoint issuing 30-day session\n```\n\n</details>\n\n### 2. Add login endpoint issuing 30-day session\n\n- **id:** `auth-login-session`\n- **complexity:** M (~half a day) — Introduces the new sessions table plus the code that reads and writes it, matching the M clause.\n- **dependencies:** `auth-signup-api`\n- **requirements:** FR-1\n- **wave:** 2\n\nCreate POST /api/auth/login that verifies credentials and issues a session token valid 30 days. Adds the sessions table. Acceptance: correct credentials return 200 with a token and a sessions row expiring ~30 days out; wrong password returns 401.\n\n<details><summary>Agent prompt</summary>\n\n```text\nImplement login and session issuance. Create a `sessions` table (id UUID PK, user_id FK users, token_hash TEXT NOT NULL, expires_at TIMESTAMPTZ NOT NULL, created_at TIMESTAMPTZ) and POST /api/auth/login accepting {email, password}. Verify the bcrypt hash; on success generate a random session token, store only its hash in sessions with expires_at = now + 30 days, and return the raw token to the client (httpOnly cookie or response body). On failure return 401 with a generic message that does not reveal whether the email or password was wrong. Done when: valid login returns 200 with a token and a corresponding sessions row expiring ~30 days later; invalid credentials return 401.\n\nAlready done (do not rebuild): Add email/password signup endpoint\nNot your scope (handled separately): Add session-authentication middleware\n```\n\n</details>\n\n### 3. Add session-authentication middleware\n\n- **id:** `auth-middleware`\n- **complexity:** S (≤1h) — Single middleware file, no new schema, no new dependency — sub-hour work.\n- **dependencies:** `auth-login-session`\n- **requirements:** FR-1\n- **wave:** 3\n\nBuild middleware that resolves the authenticated user from a session token and rejects invalid or expired sessions. Acceptance: requests without a valid, unexpired session return 401; valid sessions attach the correct user to the request.\n\n<details><summary>Agent prompt</summary>\n\n```text\nImplement authentication middleware. Add a requireAuth middleware function that reads the session token from the request, hashes it, looks it up in the `sessions` table, checks expires_at > now, and attaches the resolved user (e.g. req.user) before calling next(). If the token is missing, unknown, or expired, respond 401 and do not call next(). Handle malformed tokens without throwing an unhandled error. Done when: a request with no token returns 401; a request with an expired session's token returns 401; a request with a valid unexpired session succeeds and req.user matches the session's user_id.\n\nAlready done (do not rebuild): Add login endpoint issuing 30-day session\nNot your scope (handled separately): Add group creation endpoint and schema; Add per-notification-type preference settings\n```\n\n</details>\n\n### 4. Add group creation endpoint and schema\n\n- **id:** `groups-create-api`\n- **complexity:** M (~half a day) — Introduces the new groups and group_members tables plus the code that reads and writes them.\n- **dependencies:** `auth-middleware`\n- **requirements:** FR-2\n- **wave:** 4\n\nCreate POST /api/groups that creates a group with the creator as owner and first member. Adds groups and group_members tables. Acceptance: creation returns 201 with a group id and a group_members row for the creator with role='owner'.\n\n<details><summary>Agent prompt</summary>\n\n```text\nImplement group creation. Create a `groups` table (id UUID PK, name TEXT NOT NULL, base_currency TEXT NOT NULL, owner_id FK users, created_at) and a `group_members` table (group_id FK groups, user_id FK users, role TEXT CHECK IN ('owner','member'), joined_at, PRIMARY KEY(group_id, user_id)). Add authenticated POST /api/groups accepting {name, base_currency}. In one transaction, insert the group with owner_id = the caller, and insert a group_members row for the caller with role='owner'. Validate name and base_currency are non-empty, returning 400 otherwise. Done when: a valid request returns 201 with the group id, and a group_members row exists for the caller with role='owner'; a missing name returns 400.\n\nAlready done (do not rebuild): Add session-authentication middleware\nNot your scope (handled separately): Add group-membership authorization middleware\n```\n\n</details>\n\n### 5. Add per-notification-type preference settings\n\n- **id:** `notifications-preferences-api`\n- **complexity:** M (~half a day) — Introduces the new notification_preferences table plus the code that reads and writes it.\n- **dependencies:** `auth-middleware`\n- **requirements:** FR-8\n- **wave:** 4\n\nCreate GET/PATCH /api/users/me/notification-preferences for the three switchable notification types. Adds the notification_preferences table. Acceptance: a new user reads all three as true by default; a PATCH persists an individual toggle.\n\n<details><summary>Agent prompt</summary>\n\n```text\nImplement notification preference settings. Create a `notification_preferences` table (user_id PK/FK users, group_added BOOLEAN NOT NULL DEFAULT true, expense_recorded BOOLEAN NOT NULL DEFAULT true, weekly_digest BOOLEAN NOT NULL DEFAULT true). Add authenticated GET /api/users/me/notification-preferences returning the caller's row (creating it with defaults on first read if absent), and PATCH /api/users/me/notification-preferences accepting any subset of {group_added, expense_recorded, weekly_digest} and updating only the provided fields. Done when: GET for a user with no existing row returns all three flags as true; PATCH {expense_recorded: false} persists, and a subsequent GET reflects expense_recorded=false while the other two remain true.\n\nAlready done (do not rebuild): Add session-authentication middleware\nNot your scope (handled separately): Send group-added and expense-recorded emails; Add weekly open-balances digest email job\n```\n\n</details>\n\n### 6. Add group-membership authorization middleware\n\n- **id:** `membership-authorization`\n- **complexity:** S (≤1h) — Single middleware file, no new schema, no new dependency — sub-hour work.\n- **dependencies:** `groups-create-api`\n- **requirements:** NFR-3\n- **wave:** 5\n\nBuild reusable middleware enforcing that only members of a group can access its group-scoped routes, without leaking group existence to non-members. Acceptance: a non-member or unknown group id returns 404; a member proceeds with membership info attached.\n\n<details><summary>Agent prompt</summary>\n\n```text\nImplement group-membership authorization. Add a requireGroupMembership middleware that, for routes with a :groupId (or :id) param, looks up group_members for req.user.id and that group id and attaches the row (e.g. req.membership) before calling next(). If no matching row exists — whether the group id is unknown or simply not the caller's group — respond 404 (not 403), so callers cannot distinguish 'wrong group' from 'no access' by guessing ids. This middleware will be reused by other group-scoped routes. Done when: a request to a group-scoped route as a non-member, and as a caller using a nonexistent group id, both return 404; a request from an actual member proceeds with req.membership populated.\n\nAlready done (do not rebuild): Add group creation endpoint and schema\nNot your scope (handled separately): Add owner-only rename, remove, transfer endpoints; Add single-use 7-day invite links; Add expense creation with three split types\n```\n\n</details>\n\n### 7. Add owner-only rename, remove, transfer endpoints\n\n- **id:** `group-owner-management-api`\n- **complexity:** M (~half a day) — A few files implementing three related endpoints on the existing schema.\n- **dependencies:** `membership-authorization`\n- **requirements:** FR-2\n- **wave:** 6\n\nCreate owner-only endpoints to rename a group, remove a member, and transfer ownership. Acceptance: non-owners get 403 on all three; a successful transfer leaves exactly one group_members row with role='owner'.\n\n<details><summary>Agent prompt</summary>\n\n```text\nImplement owner-only group management. Add PATCH /api/groups/:id accepting {name} to rename the group; DELETE /api/groups/:id/members/:userId to remove a member (must not target the current owner); POST /api/groups/:id/transfer-ownership accepting {newOwnerUserId} that, in one transaction, sets the caller's group_members.role to 'member' and the target's role to 'owner'. All three require the caller's role to be 'owner', returning 403 otherwise. Ensure the invariant of exactly one owner per group holds after every transfer. Done when: a non-owner calling any of the three endpoints gets 403; a rename updates groups.name; a transfer leaves exactly one group_members row with role='owner', now belonging to the new owner.\n\nAlready done (do not rebuild): Add group-membership authorization middleware\nNot your scope (handled separately): Add paginated group activity feed\n```\n\n</details>\n\n### 8. Add single-use 7-day invite links\n\n- **id:** `group-invitations-api`\n- **complexity:** M (~half a day) — Introduces the new invitations table plus the code that reads and writes it.\n- **dependencies:** `membership-authorization`\n- **requirements:** FR-3\n- **wave:** 6\n\nCreate owner-only invite creation and an accept endpoint that adds the accepting user to the group. Adds the invitations table. Acceptance: an invite expires after 7 days; accepting before expiry adds membership; expired or reused tokens return 410.\n\n<details><summary>Agent prompt</summary>\n\n```text\nImplement group invitations. Create an `invitations` table (id UUID PK, group_id FK groups, email TEXT, token TEXT UNIQUE NOT NULL, expires_at TIMESTAMPTZ, accepted_at TIMESTAMPTZ NULL). Add POST /api/groups/:id/invitations accepting {email}, restricted to callers whose role is 'owner', which generates a cryptographically random token (>=128 bits) and sets expires_at = now + 7 days. Add authenticated POST /api/invitations/:token/accept which, if accepted_at is null and expires_at is in the future, inserts a group_members row for the caller with role='member' and sets accepted_at = now; otherwise returns 410. Done when: an invite creates an invitations row with a 7-day expiry; accepting it once before expiry adds a group_members row and sets accepted_at; accepting again, or accepting after expiry, returns 410.\n\nAlready done (do not rebuild): Add group-membership authorization middleware\nNot your scope (handled separately): Send group-added and expense-recorded emails; Add paginated group activity feed\n```\n\n</details>\n\n### 9. Add expense creation with three split types\n\n- **id:** `expenses-create-api`\n- **complexity:** L (≥1 day) — Genuine algorithmic risk: percentage splits must reconcile to the cent via remainder distribution across integer minor units, matching the rubric's algorithmic-risk clause.\n- **dependencies:** `membership-authorization`\n- **requirements:** FR-4, NFR-1\n- **wave:** 6\n\nCreate POST /api/groups/:id/expenses persisting an expense and its splits (equal, exact, or percentage) as integer minor units. Adds expenses and expense_splits tables. Acceptance: each split type persists shares summing exactly to the total; a mismatched split returns 400 with the numeric difference.\n\n<details><summary>Agent prompt</summary>\n\n```text\nImplement expense creation with split validation. Create an `expenses` table (id, group_id FK, description TEXT, amount_minor BIGINT, currency TEXT, expense_date DATE, payer_id FK users, created_by FK users, created_at) and `expense_splits` table (id, expense_id FK, user_id FK, share_minor BIGINT). Add POST /api/groups/:id/expenses accepting {description, amount_minor, date, payer_id, split_type: 'equal'|'exact'|'percentage', splits}, restricted to group members. All amounts are integers in minor units — never floats. For 'equal', divide amount_minor evenly, distributing any leftover cents deterministically (e.g. largest-remainder or first-N members) so shares sum exactly to amount_minor. For 'percentage', convert percentages to minor-unit shares the same way. For 'exact', validate the caller-supplied shares sum to amount_minor; if they do not, return 400 including the numeric difference in the message. Reject participants who are not group members. Done when: equal, exact, and percentage requests each persist expense_splits rows summing exactly to amount_minor; a mismatched exact split returns 400 stating the difference.\n\nAlready done (do not rebuild): Add group-membership authorization middleware\nNot your scope (handled separately): Add expense edit/delete with audit trail; Add per-counterparty group balances endpoint; Send group-added and expense-recorded emails; Add CSV export of group expense history\n```\n\n</details>\n\n### 10. Add expense edit/delete with audit trail\n\n- **id:** `expenses-edit-delete-api`\n- **complexity:** M (~half a day) — A few files (routes plus a new audit_log table) built on the existing expenses schema.\n- **dependencies:** `expenses-create-api`\n- **requirements:** FR-5\n- **wave:** 7\n\nCreate PUT and DELETE endpoints for an expense, writing every change to an append-only audit trail. Adds the audit_log table. Acceptance: an edit updates splits and inserts an audit_log row; a delete inserts an audit_log row; no route ever updates or deletes an audit_log row.\n\n<details><summary>Agent prompt</summary>\n\n```text\nImplement expense editing, deletion, and an immutable audit trail. Create an `audit_log` table (id, group_id FK, expense_id FK, actor_id FK users, action TEXT CHECK IN ('update','delete'), diff JSONB, created_at) with no UPDATE or DELETE route ever exposed for it. Add PUT /api/expenses/:id (any group member may call it) that re-validates the new splits sum to amount_minor exactly (same rule as expense creation), updates expenses/expense_splits, and inserts an audit_log row capturing a before/after diff. Add DELETE /api/expenses/:id that removes (or soft-deletes) the expense and inserts an audit_log row with action='delete'. Both must 404 for callers who are not members of the expense's group. Done when: a valid edit updates the splits and creates a matching audit_log row with the actor and diff; a delete removes the expense and creates an audit_log row; a mismatched edit split returns 400.\n\nAlready done (do not rebuild): Add expense creation with three split types\nNot your scope (handled separately): Add paginated group activity feed\n```\n\n</details>\n\n### 11. Add per-counterparty group balances endpoint\n\n- **id:** `balances-view-api`\n- **complexity:** L (≥1 day) — Cross-cutting aggregation across expenses, splits, and settlements under a strict 500ms p95 performance requirement, matching the rubric's cross-cutting/integration-risk clause.\n- **dependencies:** `expenses-create-api`\n- **requirements:** FR-6, NFR-2\n- **wave:** 7\n\nCreate GET /api/groups/:id/balances returning each member's owed/owing amounts per counterparty, computed in SQL from expenses, splits, and settlements. Acceptance: balances sum to zero for a group; the endpoint meets the 500ms p95 target at 20 members/5,000 expenses.\n\n<details><summary>Agent prompt</summary>\n\n```text\nImplement the group balances view. Add GET /api/groups/:id/balances (member-only) that computes, per member, their net position against every other counterparty in the group by aggregating expense_splits (payer credited, participants debited) minus recorded settlements. Compute entirely in integer minor units in a single SQL aggregation (no per-row application-loop over thousands of expenses) and add indexes such as on expenses(group_id) and expense_splits(expense_id, user_id) to hit the performance target. Response should list, per counterparty pair, the net amount owed. Done when: for a seeded group the returned balances sum to zero across the group; against a fixture of 20 members and 5,000 expenses, the endpoint responds within 500ms at the 95th percentile.\n\nAlready done (do not rebuild): Add expense creation with three split types\nNot your scope (handled separately): Implement minimum-payment settlement algorithm; Add endpoint to record a settlement payment; Add weekly open-balances digest email job\n```\n\n</details>\n\n### 12. Send group-added and expense-recorded emails\n\n- **id:** `notifications-event-emails`\n- **complexity:** M (~half a day) — A few files wiring an external email-provider integration into two existing flows, no new schema.\n- **dependencies:** `group-invitations-api`, `expenses-create-api`, `notifications-preferences-api`\n- **requirements:** FR-8\n- **wave:** 7\n\nSend transactional emails when a user joins a group and when an expense involving them is recorded, respecting their preferences. Acceptance: sending is skipped when the relevant preference is false; expense emails go only to the payer and split participants.\n\n<details><summary>Agent prompt</summary>\n\n```text\nImplement event-triggered notification emails. Add a notification service (e.g. src/services/notifications.ts) with sendGroupAddedEmail(userId, groupId) and sendExpenseRecordedEmail(expenseId), using an email-provider client (e.g. nodemailer/SES), swappable for a mock/stub in tests. Call sendGroupAddedEmail when an invitation is accepted, and call sendExpenseRecordedEmail after an expense is created, targeting only the payer and split participants. Before sending, check the target user's notification_preferences (group_added / expense_recorded respectively) and skip silently if false. Done when: accepting an invitation for a user with group_added=true triggers exactly one send call, and none for group_added=false; creating an expense sends only to payer/participants with expense_recorded=true, and none to opted-out members or non-participants.\n\nAlready done (do not rebuild): Add single-use 7-day invite links; Add expense creation with three split types; Add per-notification-type preference settings\nNot your scope (handled separately): nothing depends on this task.\n```\n\n</details>\n\n### 13. Add CSV export of group expense history\n\n- **id:** `csv-export-api`\n- **complexity:** S (≤1h) — One endpoint on existing schema, no new table, no new dependency.\n- **dependencies:** `expenses-create-api`\n- **requirements:** FR-9\n- **wave:** 7\n\nCreate GET /api/groups/:id/export streaming a CSV of the group's expenses over an optional date range, one row per expense and one column per member's share. Acceptance: an unranged export includes every expense; a date range excludes expenses outside it.\n\n<details><summary>Agent prompt</summary>\n\n```text\nImplement CSV expense export. Add GET /api/groups/:id/export?from=&to=&format=csv (member-only) that streams a text/csv response with a header row (description, date, payer, amount, then one column per current group member) and one data row per expense in the group, filling each member's column with that member's expense_splits share for the row (blank or 0 if they weren't a participant). If `from`/`to` (ISO dates) are provided, filter expenses whose expense_date falls inclusively within the range; otherwise return the full history. Render amounts from their integer-minor-unit values without floating-point rounding artifacts. Done when: a request with no date range returns one CSV row per expense in the group with one column per member; a narrowed date range excludes expenses outside it.\n\nAlready done (do not rebuild): Add expense creation with three split types\nNot your scope (handled separately): nothing depends on this task.\n```\n\n</details>\n\n### 14. Implement minimum-payment settlement algorithm\n\n- **id:** `settlement-plan-algorithm`\n- **complexity:** L (≥1 day) — Implements a settlement/scheduling algorithm, explicitly named in the rubric's L clause as genuine algorithmic risk.\n- **dependencies:** `balances-view-api`\n- **requirements:** FR-7, NFR-4\n- **wave:** 8\n\nImplement a pure function computing the minimum set of payments zeroing all balances, exposed via GET /api/groups/:id/settlement, with property-based tests. Acceptance: for randomized balance sets the plan zeroes every balance and no one pays more than their debt.\n\n<details><summary>Agent prompt</summary>\n\n```text\nImplement the settlement-plan algorithm. Add a pure function computeSettlementPlan(balances: {userId, netMinor}[]) -> {from, to, amountMinor}[] (e.g. src/services/settlement.ts) using a greedy max-debtor/max-creditor matching approach to minimize the number of payments, operating only on integer minor units. Expose it via GET /api/groups/:id/settlement (member-only), feeding it the group's current balances. Add property-based tests (e.g. with fast-check) generating random balance sets (that sum to zero) and asserting: after applying the returned plan, every balance is zero, and no payer's total payments exceed their original debt. Done when: the property-based test suite passes across many randomized balance sets, and the GET endpoint returns a valid plan for a real seeded group.\n\nAlready done (do not rebuild): Add per-counterparty group balances endpoint\nNot your scope (handled separately): nothing depends on this task.\n```\n\n</details>\n\n### 15. Add endpoint to record a settlement payment\n\n- **id:** `settlement-record-payment-api`\n- **complexity:** M (~half a day) — Introduces the new settlements table plus the code that reads and writes it.\n- **dependencies:** `balances-view-api`\n- **requirements:** FR-7, NFR-1\n- **wave:** 8\n\nCreate POST /api/groups/:id/settlements recording a payment between two members, adjusting balances the same way an expense does. Adds the settlements table. Acceptance: recording a payment equal to an existing debt zeroes that pair's balance on a subsequent balances call.\n\n<details><summary>Agent prompt</summary>\n\n```text\nImplement settlement-payment recording. Create a `settlements` table (id, group_id FK, payer_id FK users, payee_id FK users, amount_minor BIGINT, settled_at DATE, created_by FK users, created_at). Add POST /api/groups/:id/settlements (member-only) accepting {payer_id, payee_id, amount_minor, date}, validating amount_minor is a positive integer and both users are members of the group. This task assumes a balances endpoint already aggregates expense_splits into per-counterparty net amounts — extend that same aggregation to also subtract recorded settlements between the relevant pair. Done when: recording a settlement equal to an existing debt between two members results in that pair's balance reading zero on the balances endpoint afterward; a non-member payer/payee or non-positive amount returns 400.\n\nAlready done (do not rebuild): Add per-counterparty group balances endpoint\nNot your scope (handled separately): Add paginated group activity feed\n```\n\n</details>\n\n### 16. Add weekly open-balances digest email job\n\n- **id:** `weekly-digest-job`\n- **complexity:** M (~half a day) — A few files (cron wiring plus digest composition) reusing existing balance and preference logic, no new schema.\n- **dependencies:** `balances-view-api`, `notifications-preferences-api`\n- **requirements:** FR-8\n- **wave:** 8\n\nBuild a scheduled job emailing each opted-in member a summary of their open balances across all groups. Acceptance: running the job sends exactly one email per opted-in user, none to opted-out users.\n\n<details><summary>Agent prompt</summary>\n\n```text\nImplement the weekly balances digest job. Add a scheduled job (e.g. src/jobs/weeklyDigest.ts, run via node-cron or an equivalent scheduler) that, for every user with notification_preferences.weekly_digest=true, computes their current open balances across all groups they belong to (reuse the existing balances aggregation logic) and sends a single email summarizing those balances. Users with weekly_digest=false must receive nothing. Done when: invoking the job function against a fixture of users with mixed weekly_digest flags results in exactly one email sent per opted-in user, containing their balances across all their groups, and zero emails to opted-out users.\n\nAlready done (do not rebuild): Add per-counterparty group balances endpoint; Add per-notification-type preference settings\nNot your scope (handled separately): nothing depends on this task.\n```\n\n</details>\n\n### 17. Add paginated group activity feed\n\n- **id:** `activity-feed-api`\n- **complexity:** M (~half a day) — A few files merging several existing event sources into one paginated query, no new table required.\n- **dependencies:** `group-owner-management-api`, `group-invitations-api`, `expenses-edit-delete-api`, `settlement-record-payment-api`\n- **requirements:** FR-10\n- **wave:** 9\n\nCreate GET /api/groups/:id/activity merging expenses, edits/deletes, settlements, and membership changes into one reverse-chronological, cursor-paginated feed of 50 entries per page. Acceptance: a group with over 50 events returns exactly 50 newest entries plus a working next-page cursor.\n\n<details><summary>Agent prompt</summary>\n\n```text\nImplement the group activity feed. Add GET /api/groups/:id/activity?cursor=&limit=50 (member-only, limit capped at 50) that merges rows from audit_log (expense creates/edits/deletes), settlements, and membership-change events (invitation accepted, member removed, ownership transferred, group renamed) into one list ordered by created_at descending. Each returned entry must include its type (expense|edit|settlement|membership), the actor, a human-readable summary, and its timestamp. Return a cursor for fetching the next page. Done when: a group seeded with more than 50 mixed events returns exactly the 50 newest entries in correct reverse-chronological order, and following the returned cursor fetches the next 50 in the correct order.\n\nAlready done (do not rebuild): Add owner-only rename, remove, transfer endpoints; Add single-use 7-day invite links; Add expense edit/delete with audit trail; Add endpoint to record a settlement payment\nNot your scope (handled separately): nothing depends on this task.\n```\n\n</details>\n",
  "meta": {
    "cache": "miss",
    "cached_at": "2026-08-15T05:43:46+00:00",
    "duration_ms": 213588,
    "input_tokens": 2,
    "model": "claude-sonnet-5",
    "output_tokens": 23091,
    "page_count": 2,
    "plan_id": "44d249ee66ea861a",
    "prompt_version": "v1",
    "provider": "cli",
    "requirement_count": 14,
    "source_filename": "sample-prd.pdf",
    "task_count": 17
  },
  "plan_id": "44d249ee66ea861a",
  "project_summary": "SplitTab is a mobile-first web app for small groups to log shared expenses, see always-accurate per-member balances, and get the minimum set of payments that settles the group. The hardest part is the money model: every amount and split must be integer minor units that reconcile exactly to the cent across equal/exact/percentage splits, edits, and a settlement algorithm whose correctness is proven with property-based tests.",
  "requirements": [
    {
      "category": "auth",
      "id": "FR-1",
      "text": "A user signs up with email and password, and signs in to receive a session. Passwords are stored hashed. Sessions expire after 30 days."
    },
    {
      "category": "groups",
      "id": "FR-2",
      "text": "A signed-in user can create a group with a name and a base currency. The creator becomes the owner and the first member. There is exactly one owner and ownership can be transferred; the owner can rename the group and remove members."
    },
    {
      "category": "invitations",
      "id": "FR-3",
      "text": "An owner can invite a member by email. The invitation produces a single-use link valid for 7 days. Accepting it adds the user to the group."
    },
    {
      "category": "expenses",
      "id": "FR-4",
      "text": "A member records an expense with description, amount, date, payer, and shared members, split equally, by exact amounts, or by percentage shares. Splits must sum to the total; a mismatch is rejected with the difference."
    },
    {
      "category": "expenses",
      "id": "FR-5",
      "text": "Any member can edit or delete an expense. Every change is recorded in an immutable audit trail showing who changed what and when. Balances recompute after any edit."
    },
    {
      "category": "balances",
      "id": "FR-6",
      "text": "Each member sees, for every group, what they owe and what they are owed, per counterparty. The sum of all balances in a group is always zero."
    },
    {
      "category": "settlement",
      "id": "FR-7",
      "text": "A member can ask for a settlement plan: the minimum number of payments that brings every balance to zero. Recording a settlement payment updates balances the same way an expense does."
    },
    {
      "category": "notifications",
      "id": "FR-8",
      "text": "A member receives an email when added to a group, when an expense involving them is recorded, and a weekly digest of open balances. Each is individually switchable."
    },
    {
      "category": "export",
      "id": "FR-9",
      "text": "A member can export a group's expense history as CSV, covering an optional date range, with one row per expense and one column per member's share."
    },
    {
      "category": "activity",
      "id": "FR-10",
      "text": "A group has a reverse-chronological activity feed of expenses, edits, settlements, and membership changes, paged at 50 entries."
    },
    {
      "category": "money",
      "id": "NFR-1",
      "text": "Money is never stored or computed in floating point. All amounts are integer minor units, and every split must reconcile to the cent."
    },
    {
      "category": "performance",
      "id": "NFR-2",
      "text": "A group of 20 members with 5,000 expenses loads its balance view in under 500 ms at the 95th percentile."
    },
    {
      "category": "security",
      "id": "NFR-3",
      "text": "Every write is authorised against group membership. A user must not be able to read or alter a group they do not belong to, including by guessing an id."
    },
    {
      "category": "testing",
      "id": "NFR-4",
      "text": "The settlement algorithm is covered by property-based tests asserting that resulting balances are all zero and no member pays more than they owed."
    }
  ],
  "tasks": [
    {
      "agent_prompt": "Implement user signup. Create a `users` table (id UUID PK, email TEXT UNIQUE NOT NULL, password_hash TEXT NOT NULL, created_at TIMESTAMPTZ) and a POST /api/auth/signup endpoint accepting {email, password}. Hash the password with bcrypt (cost >=10); never store or log plaintext. Validate email format and password length >=8, returning 400 with a message on failure. Return 409 if the email already exists. On success return 201 with the created user's id and email (never the hash). Done when: a valid signup returns 201 and creates a users row with a bcrypt hash; a duplicate email returns 409; a short password returns 400.\n\nAlready done (do not rebuild): nothing — this is a starting task.\nNot your scope (handled separately): Add login endpoint issuing 30-day session",
      "complexity": "M",
      "dependencies": [],
      "description": "Create POST /api/auth/signup that creates a user with a bcrypt-hashed password. Adds the users table. Acceptance: valid signup returns 201 and a users row whose password_hash is not the plaintext; duplicate email returns 409; password under 8 chars returns 400.",
      "id": "auth-signup-api",
      "order": 1,
      "rationale": "Introduces the new users table plus the code that reads and writes it, matching the M clause.",
      "requirement_ids": [
        "FR-1"
      ],
      "title": "Add email/password signup endpoint",
      "wave": 1
    },
    {
      "agent_prompt": "Implement login and session issuance. Create a `sessions` table (id UUID PK, user_id FK users, token_hash TEXT NOT NULL, expires_at TIMESTAMPTZ NOT NULL, created_at TIMESTAMPTZ) and POST /api/auth/login accepting {email, password}. Verify the bcrypt hash; on success generate a random session token, store only its hash in sessions with expires_at = now + 30 days, and return the raw token to the client (httpOnly cookie or response body). On failure return 401 with a generic message that does not reveal whether the email or password was wrong. Done when: valid login returns 200 with a token and a corresponding sessions row expiring ~30 days later; invalid credentials return 401.\n\nAlready done (do not rebuild): Add email/password signup endpoint\nNot your scope (handled separately): Add session-authentication middleware",
      "complexity": "M",
      "dependencies": [
        "auth-signup-api"
      ],
      "description": "Create POST /api/auth/login that verifies credentials and issues a session token valid 30 days. Adds the sessions table. Acceptance: correct credentials return 200 with a token and a sessions row expiring ~30 days out; wrong password returns 401.",
      "id": "auth-login-session",
      "order": 2,
      "rationale": "Introduces the new sessions table plus the code that reads and writes it, matching the M clause.",
      "requirement_ids": [
        "FR-1"
      ],
      "title": "Add login endpoint issuing 30-day session",
      "wave": 2
    },
    {
      "agent_prompt": "Implement authentication middleware. Add a requireAuth middleware function that reads the session token from the request, hashes it, looks it up in the `sessions` table, checks expires_at > now, and attaches the resolved user (e.g. req.user) before calling next(). If the token is missing, unknown, or expired, respond 401 and do not call next(). Handle malformed tokens without throwing an unhandled error. Done when: a request with no token returns 401; a request with an expired session's token returns 401; a request with a valid unexpired session succeeds and req.user matches the session's user_id.\n\nAlready done (do not rebuild): Add login endpoint issuing 30-day session\nNot your scope (handled separately): Add group creation endpoint and schema; Add per-notification-type preference settings",
      "complexity": "S",
      "dependencies": [
        "auth-login-session"
      ],
      "description": "Build middleware that resolves the authenticated user from a session token and rejects invalid or expired sessions. Acceptance: requests without a valid, unexpired session return 401; valid sessions attach the correct user to the request.",
      "id": "auth-middleware",
      "order": 3,
      "rationale": "Single middleware file, no new schema, no new dependency — sub-hour work.",
      "requirement_ids": [
        "FR-1"
      ],
      "title": "Add session-authentication middleware",
      "wave": 3
    },
    {
      "agent_prompt": "Implement group creation. Create a `groups` table (id UUID PK, name TEXT NOT NULL, base_currency TEXT NOT NULL, owner_id FK users, created_at) and a `group_members` table (group_id FK groups, user_id FK users, role TEXT CHECK IN ('owner','member'), joined_at, PRIMARY KEY(group_id, user_id)). Add authenticated POST /api/groups accepting {name, base_currency}. In one transaction, insert the group with owner_id = the caller, and insert a group_members row for the caller with role='owner'. Validate name and base_currency are non-empty, returning 400 otherwise. Done when: a valid request returns 201 with the group id, and a group_members row exists for the caller with role='owner'; a missing name returns 400.\n\nAlready done (do not rebuild): Add session-authentication middleware\nNot your scope (handled separately): Add group-membership authorization middleware",
      "complexity": "M",
      "dependencies": [
        "auth-middleware"
      ],
      "description": "Create POST /api/groups that creates a group with the creator as owner and first member. Adds groups and group_members tables. Acceptance: creation returns 201 with a group id and a group_members row for the creator with role='owner'.",
      "id": "groups-create-api",
      "order": 4,
      "rationale": "Introduces the new groups and group_members tables plus the code that reads and writes them.",
      "requirement_ids": [
        "FR-2"
      ],
      "title": "Add group creation endpoint and schema",
      "wave": 4
    },
    {
      "agent_prompt": "Implement notification preference settings. Create a `notification_preferences` table (user_id PK/FK users, group_added BOOLEAN NOT NULL DEFAULT true, expense_recorded BOOLEAN NOT NULL DEFAULT true, weekly_digest BOOLEAN NOT NULL DEFAULT true). Add authenticated GET /api/users/me/notification-preferences returning the caller's row (creating it with defaults on first read if absent), and PATCH /api/users/me/notification-preferences accepting any subset of {group_added, expense_recorded, weekly_digest} and updating only the provided fields. Done when: GET for a user with no existing row returns all three flags as true; PATCH {expense_recorded: false} persists, and a subsequent GET reflects expense_recorded=false while the other two remain true.\n\nAlready done (do not rebuild): Add session-authentication middleware\nNot your scope (handled separately): Send group-added and expense-recorded emails; Add weekly open-balances digest email job",
      "complexity": "M",
      "dependencies": [
        "auth-middleware"
      ],
      "description": "Create GET/PATCH /api/users/me/notification-preferences for the three switchable notification types. Adds the notification_preferences table. Acceptance: a new user reads all three as true by default; a PATCH persists an individual toggle.",
      "id": "notifications-preferences-api",
      "order": 5,
      "rationale": "Introduces the new notification_preferences table plus the code that reads and writes it.",
      "requirement_ids": [
        "FR-8"
      ],
      "title": "Add per-notification-type preference settings",
      "wave": 4
    },
    {
      "agent_prompt": "Implement group-membership authorization. Add a requireGroupMembership middleware that, for routes with a :groupId (or :id) param, looks up group_members for req.user.id and that group id and attaches the row (e.g. req.membership) before calling next(). If no matching row exists — whether the group id is unknown or simply not the caller's group — respond 404 (not 403), so callers cannot distinguish 'wrong group' from 'no access' by guessing ids. This middleware will be reused by other group-scoped routes. Done when: a request to a group-scoped route as a non-member, and as a caller using a nonexistent group id, both return 404; a request from an actual member proceeds with req.membership populated.\n\nAlready done (do not rebuild): Add group creation endpoint and schema\nNot your scope (handled separately): Add owner-only rename, remove, transfer endpoints; Add single-use 7-day invite links; Add expense creation with three split types",
      "complexity": "S",
      "dependencies": [
        "groups-create-api"
      ],
      "description": "Build reusable middleware enforcing that only members of a group can access its group-scoped routes, without leaking group existence to non-members. Acceptance: a non-member or unknown group id returns 404; a member proceeds with membership info attached.",
      "id": "membership-authorization",
      "order": 6,
      "rationale": "Single middleware file, no new schema, no new dependency — sub-hour work.",
      "requirement_ids": [
        "NFR-3"
      ],
      "title": "Add group-membership authorization middleware",
      "wave": 5
    },
    {
      "agent_prompt": "Implement owner-only group management. Add PATCH /api/groups/:id accepting {name} to rename the group; DELETE /api/groups/:id/members/:userId to remove a member (must not target the current owner); POST /api/groups/:id/transfer-ownership accepting {newOwnerUserId} that, in one transaction, sets the caller's group_members.role to 'member' and the target's role to 'owner'. All three require the caller's role to be 'owner', returning 403 otherwise. Ensure the invariant of exactly one owner per group holds after every transfer. Done when: a non-owner calling any of the three endpoints gets 403; a rename updates groups.name; a transfer leaves exactly one group_members row with role='owner', now belonging to the new owner.\n\nAlready done (do not rebuild): Add group-membership authorization middleware\nNot your scope (handled separately): Add paginated group activity feed",
      "complexity": "M",
      "dependencies": [
        "membership-authorization"
      ],
      "description": "Create owner-only endpoints to rename a group, remove a member, and transfer ownership. Acceptance: non-owners get 403 on all three; a successful transfer leaves exactly one group_members row with role='owner'.",
      "id": "group-owner-management-api",
      "order": 7,
      "rationale": "A few files implementing three related endpoints on the existing schema.",
      "requirement_ids": [
        "FR-2"
      ],
      "title": "Add owner-only rename, remove, transfer endpoints",
      "wave": 6
    },
    {
      "agent_prompt": "Implement group invitations. Create an `invitations` table (id UUID PK, group_id FK groups, email TEXT, token TEXT UNIQUE NOT NULL, expires_at TIMESTAMPTZ, accepted_at TIMESTAMPTZ NULL). Add POST /api/groups/:id/invitations accepting {email}, restricted to callers whose role is 'owner', which generates a cryptographically random token (>=128 bits) and sets expires_at = now + 7 days. Add authenticated POST /api/invitations/:token/accept which, if accepted_at is null and expires_at is in the future, inserts a group_members row for the caller with role='member' and sets accepted_at = now; otherwise returns 410. Done when: an invite creates an invitations row with a 7-day expiry; accepting it once before expiry adds a group_members row and sets accepted_at; accepting again, or accepting after expiry, returns 410.\n\nAlready done (do not rebuild): Add group-membership authorization middleware\nNot your scope (handled separately): Send group-added and expense-recorded emails; Add paginated group activity feed",
      "complexity": "M",
      "dependencies": [
        "membership-authorization"
      ],
      "description": "Create owner-only invite creation and an accept endpoint that adds the accepting user to the group. Adds the invitations table. Acceptance: an invite expires after 7 days; accepting before expiry adds membership; expired or reused tokens return 410.",
      "id": "group-invitations-api",
      "order": 8,
      "rationale": "Introduces the new invitations table plus the code that reads and writes it.",
      "requirement_ids": [
        "FR-3"
      ],
      "title": "Add single-use 7-day invite links",
      "wave": 6
    },
    {
      "agent_prompt": "Implement expense creation with split validation. Create an `expenses` table (id, group_id FK, description TEXT, amount_minor BIGINT, currency TEXT, expense_date DATE, payer_id FK users, created_by FK users, created_at) and `expense_splits` table (id, expense_id FK, user_id FK, share_minor BIGINT). Add POST /api/groups/:id/expenses accepting {description, amount_minor, date, payer_id, split_type: 'equal'|'exact'|'percentage', splits}, restricted to group members. All amounts are integers in minor units — never floats. For 'equal', divide amount_minor evenly, distributing any leftover cents deterministically (e.g. largest-remainder or first-N members) so shares sum exactly to amount_minor. For 'percentage', convert percentages to minor-unit shares the same way. For 'exact', validate the caller-supplied shares sum to amount_minor; if they do not, return 400 including the numeric difference in the message. Reject participants who are not group members. Done when: equal, exact, and percentage requests each persist expense_splits rows summing exactly to amount_minor; a mismatched exact split returns 400 stating the difference.\n\nAlready done (do not rebuild): Add group-membership authorization middleware\nNot your scope (handled separately): Add expense edit/delete with audit trail; Add per-counterparty group balances endpoint; Send group-added and expense-recorded emails; Add CSV export of group expense history",
      "complexity": "L",
      "dependencies": [
        "membership-authorization"
      ],
      "description": "Create POST /api/groups/:id/expenses persisting an expense and its splits (equal, exact, or percentage) as integer minor units. Adds expenses and expense_splits tables. Acceptance: each split type persists shares summing exactly to the total; a mismatched split returns 400 with the numeric difference.",
      "id": "expenses-create-api",
      "order": 9,
      "rationale": "Genuine algorithmic risk: percentage splits must reconcile to the cent via remainder distribution across integer minor units, matching the rubric's algorithmic-risk clause.",
      "requirement_ids": [
        "FR-4",
        "NFR-1"
      ],
      "title": "Add expense creation with three split types",
      "wave": 6
    },
    {
      "agent_prompt": "Implement expense editing, deletion, and an immutable audit trail. Create an `audit_log` table (id, group_id FK, expense_id FK, actor_id FK users, action TEXT CHECK IN ('update','delete'), diff JSONB, created_at) with no UPDATE or DELETE route ever exposed for it. Add PUT /api/expenses/:id (any group member may call it) that re-validates the new splits sum to amount_minor exactly (same rule as expense creation), updates expenses/expense_splits, and inserts an audit_log row capturing a before/after diff. Add DELETE /api/expenses/:id that removes (or soft-deletes) the expense and inserts an audit_log row with action='delete'. Both must 404 for callers who are not members of the expense's group. Done when: a valid edit updates the splits and creates a matching audit_log row with the actor and diff; a delete removes the expense and creates an audit_log row; a mismatched edit split returns 400.\n\nAlready done (do not rebuild): Add expense creation with three split types\nNot your scope (handled separately): Add paginated group activity feed",
      "complexity": "M",
      "dependencies": [
        "expenses-create-api"
      ],
      "description": "Create PUT and DELETE endpoints for an expense, writing every change to an append-only audit trail. Adds the audit_log table. Acceptance: an edit updates splits and inserts an audit_log row; a delete inserts an audit_log row; no route ever updates or deletes an audit_log row.",
      "id": "expenses-edit-delete-api",
      "order": 10,
      "rationale": "A few files (routes plus a new audit_log table) built on the existing expenses schema.",
      "requirement_ids": [
        "FR-5"
      ],
      "title": "Add expense edit/delete with audit trail",
      "wave": 7
    },
    {
      "agent_prompt": "Implement the group balances view. Add GET /api/groups/:id/balances (member-only) that computes, per member, their net position against every other counterparty in the group by aggregating expense_splits (payer credited, participants debited) minus recorded settlements. Compute entirely in integer minor units in a single SQL aggregation (no per-row application-loop over thousands of expenses) and add indexes such as on expenses(group_id) and expense_splits(expense_id, user_id) to hit the performance target. Response should list, per counterparty pair, the net amount owed. Done when: for a seeded group the returned balances sum to zero across the group; against a fixture of 20 members and 5,000 expenses, the endpoint responds within 500ms at the 95th percentile.\n\nAlready done (do not rebuild): Add expense creation with three split types\nNot your scope (handled separately): Implement minimum-payment settlement algorithm; Add endpoint to record a settlement payment; Add weekly open-balances digest email job",
      "complexity": "L",
      "dependencies": [
        "expenses-create-api"
      ],
      "description": "Create GET /api/groups/:id/balances returning each member's owed/owing amounts per counterparty, computed in SQL from expenses, splits, and settlements. Acceptance: balances sum to zero for a group; the endpoint meets the 500ms p95 target at 20 members/5,000 expenses.",
      "id": "balances-view-api",
      "order": 11,
      "rationale": "Cross-cutting aggregation across expenses, splits, and settlements under a strict 500ms p95 performance requirement, matching the rubric's cross-cutting/integration-risk clause.",
      "requirement_ids": [
        "FR-6",
        "NFR-2"
      ],
      "title": "Add per-counterparty group balances endpoint",
      "wave": 7
    },
    {
      "agent_prompt": "Implement event-triggered notification emails. Add a notification service (e.g. src/services/notifications.ts) with sendGroupAddedEmail(userId, groupId) and sendExpenseRecordedEmail(expenseId), using an email-provider client (e.g. nodemailer/SES), swappable for a mock/stub in tests. Call sendGroupAddedEmail when an invitation is accepted, and call sendExpenseRecordedEmail after an expense is created, targeting only the payer and split participants. Before sending, check the target user's notification_preferences (group_added / expense_recorded respectively) and skip silently if false. Done when: accepting an invitation for a user with group_added=true triggers exactly one send call, and none for group_added=false; creating an expense sends only to payer/participants with expense_recorded=true, and none to opted-out members or non-participants.\n\nAlready done (do not rebuild): Add single-use 7-day invite links; Add expense creation with three split types; Add per-notification-type preference settings\nNot your scope (handled separately): nothing depends on this task.",
      "complexity": "M",
      "dependencies": [
        "group-invitations-api",
        "expenses-create-api",
        "notifications-preferences-api"
      ],
      "description": "Send transactional emails when a user joins a group and when an expense involving them is recorded, respecting their preferences. Acceptance: sending is skipped when the relevant preference is false; expense emails go only to the payer and split participants.",
      "id": "notifications-event-emails",
      "order": 12,
      "rationale": "A few files wiring an external email-provider integration into two existing flows, no new schema.",
      "requirement_ids": [
        "FR-8"
      ],
      "title": "Send group-added and expense-recorded emails",
      "wave": 7
    },
    {
      "agent_prompt": "Implement CSV expense export. Add GET /api/groups/:id/export?from=&to=&format=csv (member-only) that streams a text/csv response with a header row (description, date, payer, amount, then one column per current group member) and one data row per expense in the group, filling each member's column with that member's expense_splits share for the row (blank or 0 if they weren't a participant). If `from`/`to` (ISO dates) are provided, filter expenses whose expense_date falls inclusively within the range; otherwise return the full history. Render amounts from their integer-minor-unit values without floating-point rounding artifacts. Done when: a request with no date range returns one CSV row per expense in the group with one column per member; a narrowed date range excludes expenses outside it.\n\nAlready done (do not rebuild): Add expense creation with three split types\nNot your scope (handled separately): nothing depends on this task.",
      "complexity": "S",
      "dependencies": [
        "expenses-create-api"
      ],
      "description": "Create GET /api/groups/:id/export streaming a CSV of the group's expenses over an optional date range, one row per expense and one column per member's share. Acceptance: an unranged export includes every expense; a date range excludes expenses outside it.",
      "id": "csv-export-api",
      "order": 13,
      "rationale": "One endpoint on existing schema, no new table, no new dependency.",
      "requirement_ids": [
        "FR-9"
      ],
      "title": "Add CSV export of group expense history",
      "wave": 7
    },
    {
      "agent_prompt": "Implement the settlement-plan algorithm. Add a pure function computeSettlementPlan(balances: {userId, netMinor}[]) -> {from, to, amountMinor}[] (e.g. src/services/settlement.ts) using a greedy max-debtor/max-creditor matching approach to minimize the number of payments, operating only on integer minor units. Expose it via GET /api/groups/:id/settlement (member-only), feeding it the group's current balances. Add property-based tests (e.g. with fast-check) generating random balance sets (that sum to zero) and asserting: after applying the returned plan, every balance is zero, and no payer's total payments exceed their original debt. Done when: the property-based test suite passes across many randomized balance sets, and the GET endpoint returns a valid plan for a real seeded group.\n\nAlready done (do not rebuild): Add per-counterparty group balances endpoint\nNot your scope (handled separately): nothing depends on this task.",
      "complexity": "L",
      "dependencies": [
        "balances-view-api"
      ],
      "description": "Implement a pure function computing the minimum set of payments zeroing all balances, exposed via GET /api/groups/:id/settlement, with property-based tests. Acceptance: for randomized balance sets the plan zeroes every balance and no one pays more than their debt.",
      "id": "settlement-plan-algorithm",
      "order": 14,
      "rationale": "Implements a settlement/scheduling algorithm, explicitly named in the rubric's L clause as genuine algorithmic risk.",
      "requirement_ids": [
        "FR-7",
        "NFR-4"
      ],
      "title": "Implement minimum-payment settlement algorithm",
      "wave": 8
    },
    {
      "agent_prompt": "Implement settlement-payment recording. Create a `settlements` table (id, group_id FK, payer_id FK users, payee_id FK users, amount_minor BIGINT, settled_at DATE, created_by FK users, created_at). Add POST /api/groups/:id/settlements (member-only) accepting {payer_id, payee_id, amount_minor, date}, validating amount_minor is a positive integer and both users are members of the group. This task assumes a balances endpoint already aggregates expense_splits into per-counterparty net amounts — extend that same aggregation to also subtract recorded settlements between the relevant pair. Done when: recording a settlement equal to an existing debt between two members results in that pair's balance reading zero on the balances endpoint afterward; a non-member payer/payee or non-positive amount returns 400.\n\nAlready done (do not rebuild): Add per-counterparty group balances endpoint\nNot your scope (handled separately): Add paginated group activity feed",
      "complexity": "M",
      "dependencies": [
        "balances-view-api"
      ],
      "description": "Create POST /api/groups/:id/settlements recording a payment between two members, adjusting balances the same way an expense does. Adds the settlements table. Acceptance: recording a payment equal to an existing debt zeroes that pair's balance on a subsequent balances call.",
      "id": "settlement-record-payment-api",
      "order": 15,
      "rationale": "Introduces the new settlements table plus the code that reads and writes it.",
      "requirement_ids": [
        "FR-7",
        "NFR-1"
      ],
      "title": "Add endpoint to record a settlement payment",
      "wave": 8
    },
    {
      "agent_prompt": "Implement the weekly balances digest job. Add a scheduled job (e.g. src/jobs/weeklyDigest.ts, run via node-cron or an equivalent scheduler) that, for every user with notification_preferences.weekly_digest=true, computes their current open balances across all groups they belong to (reuse the existing balances aggregation logic) and sends a single email summarizing those balances. Users with weekly_digest=false must receive nothing. Done when: invoking the job function against a fixture of users with mixed weekly_digest flags results in exactly one email sent per opted-in user, containing their balances across all their groups, and zero emails to opted-out users.\n\nAlready done (do not rebuild): Add per-counterparty group balances endpoint; Add per-notification-type preference settings\nNot your scope (handled separately): nothing depends on this task.",
      "complexity": "M",
      "dependencies": [
        "balances-view-api",
        "notifications-preferences-api"
      ],
      "description": "Build a scheduled job emailing each opted-in member a summary of their open balances across all groups. Acceptance: running the job sends exactly one email per opted-in user, none to opted-out users.",
      "id": "weekly-digest-job",
      "order": 16,
      "rationale": "A few files (cron wiring plus digest composition) reusing existing balance and preference logic, no new schema.",
      "requirement_ids": [
        "FR-8"
      ],
      "title": "Add weekly open-balances digest email job",
      "wave": 8
    },
    {
      "agent_prompt": "Implement the group activity feed. Add GET /api/groups/:id/activity?cursor=&limit=50 (member-only, limit capped at 50) that merges rows from audit_log (expense creates/edits/deletes), settlements, and membership-change events (invitation accepted, member removed, ownership transferred, group renamed) into one list ordered by created_at descending. Each returned entry must include its type (expense|edit|settlement|membership), the actor, a human-readable summary, and its timestamp. Return a cursor for fetching the next page. Done when: a group seeded with more than 50 mixed events returns exactly the 50 newest entries in correct reverse-chronological order, and following the returned cursor fetches the next 50 in the correct order.\n\nAlready done (do not rebuild): Add owner-only rename, remove, transfer endpoints; Add single-use 7-day invite links; Add expense edit/delete with audit trail; Add endpoint to record a settlement payment\nNot your scope (handled separately): nothing depends on this task.",
      "complexity": "M",
      "dependencies": [
        "group-owner-management-api",
        "group-invitations-api",
        "expenses-edit-delete-api",
        "settlement-record-payment-api"
      ],
      "description": "Create GET /api/groups/:id/activity merging expenses, edits/deletes, settlements, and membership changes into one reverse-chronological, cursor-paginated feed of 50 entries per page. Acceptance: a group with over 50 events returns exactly 50 newest entries plus a working next-page cursor.",
      "id": "activity-feed-api",
      "order": 17,
      "rationale": "A few files merging several existing event sources into one paginated query, no new table required.",
      "requirement_ids": [
        "FR-10"
      ],
      "title": "Add paginated group activity feed",
      "wave": 9
    }
  ],
  "warnings": [],
  "waves": [
    [
      "auth-signup-api"
    ],
    [
      "auth-login-session"
    ],
    [
      "auth-middleware"
    ],
    [
      "groups-create-api",
      "notifications-preferences-api"
    ],
    [
      "membership-authorization"
    ],
    [
      "group-owner-management-api",
      "group-invitations-api",
      "expenses-create-api"
    ],
    [
      "expenses-edit-delete-api",
      "balances-view-api",
      "notifications-event-emails",
      "csv-export-api"
    ],
    [
      "settlement-plan-algorithm",
      "settlement-record-payment-api",
      "weekly-digest-job"
    ],
    [
      "activity-feed-api"
    ]
  ]
}
